import ForkRightIcon from '@mui/icons-material/ForkRight';
import { blueGrey } from '@mui/material/colors';

/**
 * Renders the forks icon.
 */
export function ExplorerForksIcon() {
  return (
    <ForkRightIcon fontSize="small" sx={{ mr: 0.5, color: blueGrey[500] }} />
  );
}
