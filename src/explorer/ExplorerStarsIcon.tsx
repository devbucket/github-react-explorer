import StarIcon from '@mui/icons-material/Star';
import { amber } from '@mui/material/colors';

/**
 * Renders the stars icon.
 */
export function ExplorerStarsIcon() {
  return <StarIcon fontSize="small" sx={{ mr: 0.5, color: amber[500] }} />;
}
